# 의사결정 기록

AskUserQuestion으로 오간 질문과 선택을 자동 기록한다. 고른 것뿐 아니라 버린 선택지도 남으므로 왜 그렇게 정했는지가 추적된다.

## 2026-09-07 19:20 · 마무리

**Q.** 밀도 계획 실행이 끝났습니다. 브랜치 `feat/tdd-instruction-density`(11 커밋, 린트 32/32·훅 통과, 최종 리뷰·재리뷰 통과)를 어떻게 마무리할까요?

선택지:

**→** push 후 PR 생성 (추천) — origin에 push하고 gx-pull-request 스킬로 PR을 만듭니다. 본문 초안(배경·변경·판정 9건·체크리스트)은 준비돼 있습니다. 머지는 별도 지시 때만
- 로컬에서 main에 머지 — PR 없이 main에 머지하고 브랜치를 삭제합니다. release.yml은 main push 시 태그를 만듭니다
- 그대로 둠 — 브랜치를 남겨 두고 직접 검토한 뒤 별도 지시

**A.** push 후 PR 생성 (추천)
